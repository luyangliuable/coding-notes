extern crate time;

use time::PrimitiveDateTime as DateTime;

// Returns a DateTime one billion seconds after start.
pub fn after() -> DateTime {
    unimplemented!();
    // let now = SystemTime::now();

    // let current_time: time::PrimitiveDateTime<Local> = now.into();

    // let gigasecond = Duration::new(1_000_000_000, 0);

    // let gigasecond_from_now = now + gigasecond;
    // gigasecond_from_now
}
